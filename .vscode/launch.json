{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "GO Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "env": {
                "ENVIRONMENT": "local",
                "GOOGLE_APPLICATION_CREDENTIALS": "credentials.local.json",
            }
        }
    ]
}